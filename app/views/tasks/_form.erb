<% form_for @task, :html => {:class => nil, :id => nil} do |form| %>
   <%= form.error_messages %>
   <%= form.label :text, "What's the task:" %>
   <%= form.text_area :text, :cols => nil, :rows => nil %>
   <%= form.submit "Update" %> | <%= link_to "Cancel", @task, :class => "cancel" %>
<% end %>