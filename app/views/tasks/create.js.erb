try {
  $("task_<%= @task.higher_item.id %>").insert({after: "<%= escape_javascript(render @task) %>"});
  $("task_<%= @task.id %>").hide().slideDown(function(e){
    new Taskar.FX.ScrollTo(e.element);
  });
} catch(e){
	console.log(e);
}