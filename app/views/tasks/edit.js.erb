<% javascript_response do %>
  (function(element){
    element.update("<%= escape_javascript(render "form") %>");
    element.highlight();
    element.down('textarea').focus();
  })($('task_<%= @task.id %>') || $('task_title'));
<% end %>