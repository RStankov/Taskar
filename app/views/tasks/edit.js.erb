<% javascript_response do %>
  (function(element){
    element.update("<%= escape_javascript(render "form", :task => @task) %>");
    element.highlight();
    element.down('textarea').focus();
  })($('task_<%= @task.id %>') || $('task_content'));
<% end %>