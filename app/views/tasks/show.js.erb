<% javascript_response do %>
  (function(){
    var element = $('task_title');
    if (element){
      element.replace("<%= escape_javascript(render 'task_title') %>");
    } else {
      element = $('task_<%= @task.id %>');
      element.replace("<%= escape_javascript(render @task) %>");
    }
    element.highlight();
  })();
<% end %>

