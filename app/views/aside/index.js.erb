<%= javascript_response do %>
  Taskar.Sections.Aside.updateResponsibilities("<%= escape_javascript participant_task_count(@responsibilities_count) %>");
  <% @participants.each do |participant| %>
    Taskar.Sections.Aside.updateParticipant("<%= dom_id(participant) %>", {
      title:  "<%= escape_javascript participant_status_title(participant)  %>",
      status: "<%= escape_javascript participant_status(participant) %>"
    });
  <% end %>
  Taskar.Sections.Aside.updateEvents(<%= @unseen_events_count %>, "<%= t("aside.unseen_events", :count => @unseen_events_count) %>");
<% end %>