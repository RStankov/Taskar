<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
	  <%= csrf_meta_tag %>
    <title><%= @content_for_title ? "Taskar: #{@content_for_title}" : "Taskar" %></title>
  	<%= stylesheet_link_tag 'application' %>
  </head>
  <body>
  	<%= render "shared/header" if current_user %>
		<div id="outside_box">
    	<%= yield %>
		</div>
  </body>
</html>