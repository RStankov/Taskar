<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Passwords</title>
  </head>
  <body>
    <%= '<p style="color: green">' + notice + '</p>' if notice %>
    <%= '<p style="color: red">' + alert + '</p>' if alert %>
    <%= yield %>
  </body>
</html>