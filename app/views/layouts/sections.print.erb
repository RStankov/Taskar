<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=t :project %>: <%= @project.name %><%= " / #{@section.name}" if @section %></title>
  	<%= stylesheet_link_tag 'print', :media => 'all' %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>