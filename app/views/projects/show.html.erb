<h1><%=h @project.name %></h1>

<p>
  <%= link_to t(:edit), edit_project_path(@project) %> |
  <%= link_to_delete t(:delete), @project, :confirm => t(:'projects.confirm') %>
</p>

<h3><%=t :'projects.participants' %></h3>
<ul>
  <% @project.users.each do |user| %>
    <li><%= link_to h(user.full_name), user %></li>
  <% end %>
</ul>

<h3><%=t :'projects.sections' %></h3>
<ul>
  <% @project.sections.each do |section| %>
    <li><%= link_to h(section.name), section %></li>
  <% end %>
</ul>
